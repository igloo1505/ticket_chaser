"use server"


const Home = async () => {
    /* const { user } = await validateOrRedirect(); */
    /* <AuthObserver user={user} /> */
    return (
        <div>
            Home page here
        </div>
    )
};

export default Home;
